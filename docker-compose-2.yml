services:
  virtual-circuit-breaker:
    build: ./virtual-circuit-breaker
    environment:
    - TZ=Europe/Madrid
    - INTERFACE=eth0
    - APPID=0x4100
    - NS=120000
    privileged: true
    container_name: XBCR-2
    image: virtualizaciondenodoslogicos-docker-dev-local.artifact.tecnalia.dev:443/ied2-substation_virtual_nodes-virtual-circuit-breaker:latest
    networks:
    - Cable_Network
networks:
  Cable_Network:
    driver: bridge
